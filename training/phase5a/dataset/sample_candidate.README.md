# sample_candidate.json

This file illustrates the exact JSON shape that `generate_candidates.py` produces and that the HDNA browser extension's Trial 4 Training Review panel expects to import. It is **not real generated data** — it is a format-reference fixture, committed to the repository for documentation purposes only. Do not use this as training data.
