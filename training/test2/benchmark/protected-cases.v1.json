{
  "version": "v1",
  "description": "Hashes only — never raw text. Any case whose normalized (originalText, finalText) pair hash appears here must never enter Test 2 generation, verification, or training prompts. Populate via pipeline/add_protected_case.py before generation begins.",
  "test1_final_benchmark_status": "0 of 10 populated — the final Test 1 benchmark (training/phase5a/benchmark/test1-final-result.md, 'Final validation round') is operator-local and not present anywhere in this repository (tracked or untracked), by evaluation-integrity design. Populate this registry from that local file with add_protected_case.py before running real Test 2 generation.",
  "protected_pair_hashes": []
}
